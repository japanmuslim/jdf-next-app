import Layout from '@/layouts/Layout';

export default function Contact() {
  return (
    <Layout pageTitle="Contact" pageDescription="Contact page">
      <section>Contact</section>
    </Layout>
  );
}
